import s from "../../sin"

/**
 * @type {S.Component<Demo.Foo, { func: () => void }>}
 */
const component = s((attrs, children, context) => {

  attrs.
  context.

})


component({
  label: '',
  loading: true,

})